name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 41.235.205.92.host.secureserver.net
          username: ctiehs
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            /var/www/hometracker/deploy/deploy.sh
